"""Activity-level metric builders for local DB-derived data.

Each function accepts prepared inputs (stream_data, session_data, athlete, etc.)
and returns plain dicts matching the response models in activities.schemas.
"""

